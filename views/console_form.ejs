<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Add new console</title>
    </head>
    <body>
        <h1>Add new console to the listing</h1>
        <form action="" method="post">
            <p>
                <label for="name">Name of the console: </label>
                <input type="text" id="name" name="name" required minlength="1"
                maxlength="100" value="<%= console ? console.name : ""%>" />
            </p>
            <p>
                <label for="price">Price: </label>
                <input type="number" id="price" name="price" required min="1"
                value="<%= console ? console.price : ""%>"/>
            </p>
            <p>
                <label for="quantity">Quantity: </label>
                <input type="number" id="quantity" name="quantity" required
                min="1" value="<%= console ? console.quantity : ""%>" />
            </p>
            <p>
                <label for="name">Manufacturer: </label>
                <input type="text" id="name" name="name" required minlength="1"
                maxlength="100" value="<%= console ? console.manufacturer :
                ""%>" />
            </p>
            <button type="submit">Submit</button>
        </form>
    </body>
</html>
